# STATUS

One box per milestone. See PLAN.md for done-criteria, CLAUDE.md for the claim/spine/invariants.

- [x] **M0** — scaffolding + plan + provenance + git
- [x] **M1** — robustness instrumentation: salience knob (`switch_contrast` + `--switch-contrast`/`--contrasts`); independent rollout regret (`control_regret_rollout`, probe regret kept as `control_regret_probe`); eff_rank in every JSON + figure + loud `<5 @ p=0.5` warning; `src/aggregate.py` (mean ±95% CI per (model,p) + JEPA-vs-p non-monotonicity flag); named presets via `--config` (`salience_sweep`, `robustness`) + `--smoke`. (This prompt redefined M1 from the provisional config/CLU plan; `--config` presets landed, YAML/`latent_dim` deferred.)
- [x] **M2** — real mutual-information estimator: `src/information.py` (`estimate_mi_infonce(Z, c, *, epochs, device) -> nats`, separable InfoNCE critic on a held-out split so the bound stays honest); MI sanity test in `verify.py` (recoverable binary c -> ~log 2, independent -> ~0); `mi_infonce` added to every result JSON (`mi_epochs` config knob); figure 3 de-circularized: x = estimated I(Z;c), y = rollout regret (no more regret = 1 - probe vs probe). (This prompt redefined M2 from the provisional cosine+var+cov plan; that JEPA invariant work is deferred.)
- [x] **M3** — quadrant env + objective registry + matrix: `src/quadrant_env.py` (`FeatureSpec`, `QuadrantEnv`, `make_cell`; 12×12 frame, all 4 cells of the 2×2, controllable=XOR-toggle / exogenous=`predictability` knob, reward = `1[action==a*]`, per-feature `labels`); `src/models.py` extended into an **objective registry** (`OBJECTIVES`: recon/jepa/jepa_ctrl/jepa_invdyn/jepa_reward/oracle) with `JEPARewardModel` (reward head, the proposed fix) — encoder byte-identical across all (asserted in `quadrant_experiment.py` and `verify.py`); `Encoder`/`Decoder` now size-correct for 12×12 (`_conv_out_size` + decoder interpolate; 32×32 unchanged); generic `train_objective` + `TransitionDataset` in `training.py` (legacy `train_model` untouched); `quadrant_experiment.py` orchestrator → per-run JSONs (skip-if-done) + `results/quadrant_matrix.json` + heatmap (`figure6`). Cell-4 pattern confirmed on smoke (see RESULTS.md). (Original M3 named `GridWorldHiddenRuleEnv` for cells 1–2; this prompt builds all 4 cells in one `QuadrantEnv` instead.)
- [x] **M4** — minimal fix + capacity + 2nd env: (1) **existing-fixes-fail demo** `--config cell4_failure` runs `jepa_ctrl` + `jepa_invdyn` on **random** actions on cell 4 — both fail (quadrant: 0.64/0.71/0.71 < 0.75) while `jepa_reward`/`oracle` keep it; (2) **minimum reward signal** — `reward_label_fraction` via a per-transition `reward_mask` (masked BCE in `JEPARewardModel`; all-ones == legacy), `--config min_reward_signal` sweeps fractions and reports the smallest one crossing the pass threshold (`figure7`); (3) **capacity sweep** `--config capacity_sweep` over `latent_dim {16..1024}` × ≥3 seeds on cell 4 in quadrant **and** switch — `jepa` flat-low, `jepa_reward` flat-high → not capacity-bound (`figure8`); (4) **second env** `src/gridworld_env.py` (`GridWorldHiddenRuleEnv`: cell-4 logic in a gridworld skin) + shared `roll_transitions` in `env.py` + `--env {switch_color,gridworld,quadrant}`. `verify.py` asserts the cell-4 schema across all 3 envs and the reward-mask semantics; invariant 1 still holds across the zoo. Default matrix unchanged (legacy JSON names reused). (Bisimulation/DBC term deferred to M5.)
- [x] **M5** — analytical bisimulation + proposition numbers: `src/bisimulation.py` (`analytical_bisim_distance(env)` — the on-policy bisim distance between the feature=0/1 states of a cell-style env reduces to the immediate reward gap `1[relevant]` plus a discounted exogenous-transition term `gamma|2p-1|d`; closed-form fixed point `d = g/(1-gamma|2p-1|)` → **1.0 at p=0.5** for cell 4, 0 for irrelevant cells, `1/(1-gamma)` for a frozen feature; `latent_class_distance(Z, labels)` — **cross-fit** whitened/Mahalanobis centroid distance, ~0 for a collapsed feature, robust to high-dim in-sample inflation). `proposition_numbers.py` (`--full`/smoke; cell 4, p=0.5) trains tiny jepa/recon + oracle one-hot and emits analytical bisim vs per-objective latent class distance: smoke → analytical 1.0, oracle/AE ~2.0, **jepa 0.97 (only 48% of oracle)** → bisim error (oracle−jepa) = 1.03 > 0; writes `results/proposition_numbers_smoke.json` and an idempotent M5 block in RESULTS.md. `verify.py` grows a `bisim_sanity()` (analytical reduction on real envs + proxy separation). (This prompt redefined M5 as the bisimulation/DBC theory support deferred from M4; the provisional full-matrix/decision M5 is deferred.)
- [ ] **M6** — pretrained-encoder comparison, GUARDED download (never during build/smoke) — **SKIPPED (future work)**; harness left in place, marked in docs.
- [x] **M7** — full reproduction + report; verify.py asserts all 5 invariants end-to-end. Completed: figure9 (main paper figure), LaTeX tables (table1–3), RESULTS.md (consolidated + Observation 1 rename), REPRODUCE.md, README.md, requirements.txt pinned, STATUS.md. Commit: "M7: figures, paper tables, reproducibility, docs — TMLR-ready".
